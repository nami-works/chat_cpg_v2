# FastAPI and server
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# WebSocket support for real-time chat
websockets>=12.0

# Database - Docker-friendly (no psycopg2-binary needed)
sqlalchemy==2.0.23
alembic==1.12.1
# psycopg2-binary==2.9.7  # REMOVED - causes Windows build issues
asyncpg>=0.29.0  # Use this for Docker PostgreSQL

# Authentication & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
authlib>=1.2.1
python-dotenv==1.0.0

# Email & Communication
emails==0.6.0
jinja2==3.1.3

# Redis for caching
redis==5.0.1
aioredis==2.0.1

# HTTP requests
httpx>=0.25.2
requests==2.31.0

# File processing
python-docx>=1.1.0
PyPDF2>=3.0.1
pdfplumber==0.10.3
openpyxl>=3.1.2
pandas>=2.1.4

# Vector database and embeddings
pinecone-client>=2.2.4
openai==1.6.1
sentence-transformers==2.3.1

# AI Models for Chat
groq==0.4.1
anthropic>=0.8.0

# Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Image processing
Pillow>=10.1.0

# Async utilities
asyncio-mqtt==0.16.1

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0

# Stripe payments
stripe>=7.0.0

# Google OAuth
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# CORS (handled by FastAPI built-in middleware)
# python-cors - not needed, using FastAPI's CORSMiddleware

# Phase 3 - Knowledge Base System dependencies
# Windows-friendly file type detection
python-magic-bin>=0.4.14; sys_platform == "win32"
python-magic>=0.4.27; sys_platform != "win32"
nltk>=3.8.1

# Phase 4 - Enhanced Chat Interface dependencies
# Date and time utilities
python-dateutil>=2.8.2
# UUID utilities (built into Python standard library)
# uuid>=1.30 - not needed, uuid is built-in
# JSON utilities for chat context
jsonschema>=4.20.0
# Markdown processing for chat messages
markdown>=3.5.0
# Async file operations for chat logs
aiofiles==23.2.1

# AI and LangChain dependencies
langchain==0.1.0
langchain-openai==0.0.2
langchain-groq==0.0.1

# Additional utilities
email-validator==2.2.0 